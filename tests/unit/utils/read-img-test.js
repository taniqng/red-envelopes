import readImg from 'red-envelopes/utils/read-img';
import { module, test } from 'qunit';

module('Unit | Utility | read img');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = readImg();
  assert.ok(result);
});
